$("#new_message")[0].reset()
// $("div#chat_messages").html("<%= escape_javascript(render '/messages/messages', active_room: @chatroom, messages: @chatroom.messages ) %>");
$(function() {
    var chat_messages = document.getElementById('chat_messages');
    var height = chat_messages.scrollHeight;
    $(chat_messages).scrollTop(height);
})