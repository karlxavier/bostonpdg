<div id="sidebar" class="sidebar">
    <div class="slimScrollDiv">
        <ul class="nav">
            <li class="nav-profile">
                    <div class="cover with-shadow"></div>
                    <div class="info">
                        <%= current_user.full_name %>
                        <small><%= current_user.email %></small>
                    </div>
                </a>
            </li>
        </ul>
        <ul class="nav">
            <li class="<%= controller.class.name.split("::").first == 'DashboardsController' ? 'active' : '' %>">
                <%= link_to dashboards_path do %>
                    <i class="fas fa-stopwatch"></i> 
                    <span>Timelogs Dashboard</span> 
                <% end %>
            </li>
            <li class="<%= controller.class.name.split("::").first == 'OrdersController' ? 'active' : '' %>">
                <%= link_to orders_path do %>
                    <i class="fas fa-calendar-alt"></i> 
                    <span>Orders</span> 
                <% end %>
            </li>
            <li class="<%= controller.class.name.split("::").first == 'DocumentUploadsController' ? 'active' : '' %>">
                <%= link_to document_uploads_path do %>
                    <i class="fas fa-calendar-alt"></i> 
                    <span>Shared Documents</span> 
                <% end %>
            </li>
            <li class="<%= controller.class.name.split("::").first == 'ChannelsController' ? 'active' : '' %>">
                <%= link_to channels_path do %>
                    <i class="far fa-comments"></i> 
                    Messages
                <% end %>
            </li>
            <li class="<%= controller.class.name.split("::").first == 'UserTimeLogsController' ? 'active' : '' %>">
                <%= link_to user_time_logs_path do %>
                    <i class="far fa-clock"></i> 
                    Time Logs
                <% end %>
            </li>
        </ul>
    </div>
</div>
<script type="text/javascript">
	$(function(){
		$(document).on('click', '#sidebar', function (e) {
		  e.stopPropagation();
		});
	})
</script>
	