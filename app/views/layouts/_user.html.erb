<% if current_user.present? %>
  <%= render "layouts/static/sidebar_nav" %>
<% end %>
<div class="page active <% if (params[:controller] == 'inventories' && params[:action] != 'index') || params[:controller] == 'vendors'%>body-margin<% end %>" style="padding-bottom: 0;">
  <% if current_user.present? && params[:action] != 'index' && params[:action] != 'show' %>
      <a href="javascript:history.back()" class="badge badge-secondary btn btn-nav btn-back">Back</a>
  <% end %>
  <section class="dashboard-header section-padding">
    <div class="container-fluid">
      <%= render "layouts/static/notification" %>
      <%= yield %>
    </div>
  </section>
</div>
<div id="modal-shows"></div>
<div id="msg_preview" class="msg_modal">
  <span class="close_preview">&times;</span>
  <img class="msg_modal-content" id="img01">
</div>

<%= render "search_results/full_screen_search" %>
<script type="text/javascript">
  $(function() {
    $('a[href="#search"]').on("click", function(event) {
      event.preventDefault();
      $("#search").addClass("open");
      $('#search > form > input[type="search"]').focus();
    });

    $("#search, #search button.close, #search i.close").on("click keyup", function(event) {
      if (
        event.target == this ||
        event.target.className.includes("close") ||
        event.keyCode == 27
      ) {
        $(this).removeClass("open");
      }
    });
  });
</script>
<script>
    feather.replace({ class: 'align-middle' })
</script>